#!/usr/bin/env bash

# We can change the filename for the compile script with this option
compile="script/asset-build"

# We can set the version of Node to use for the app here
node_version=8.11.1

# We can set the version of NPM to use for the app here
npm_version=5.6.0
